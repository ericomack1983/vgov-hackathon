---
phase: 02-procurement-ai-engine
plan: 04
type: execute
wave: 2
depends_on: [02-01, 02-03]
files_modified:
  - src/app/rfp/[id]/page.tsx
  - src/components/ai/ScoreRadarChart.tsx
  - src/components/ai/ScoreBar.tsx
  - src/components/ai/RankedSupplierRow.tsx
  - src/components/ai/BestValueBadge.tsx
  - src/components/ai/DimensionScoreGrid.tsx
  - src/components/ai/ExplainabilityPanel.tsx
  - src/components/ai/OverrideForm.tsx
autonomous: true
requirements: [PROC-04, AIEN-02, AIEN-03, AIEN-04, AIEN-05, AIEN-06]

must_haves:
  truths:
    - "Triggering AI evaluation on an RFP produces a ranked supplier list with dimension scores, composite score, and Best Value badge on winner"
    - "AI explainability panel shows natural language narrative citing specific data points"
    - "Score radar chart renders 5-axis SVG polygon for supplier comparison"
    - "Gov Officer can override AI recommendation by selecting a different supplier with written justification"
    - "Run AI Evaluation button only enabled when >= 2 bids exist"
  artifacts:
    - path: "src/components/ai/ScoreRadarChart.tsx"
      provides: "5-axis SVG radar chart"
      exports: ["ScoreRadarChart"]
    - path: "src/components/ai/ScoreBar.tsx"
      provides: "Horizontal score bar 0-100"
      exports: ["ScoreBar"]
    - path: "src/components/ai/RankedSupplierRow.tsx"
      provides: "Table row with rank, scores, and winner indicator"
      exports: ["RankedSupplierRow"]
    - path: "src/components/ai/BestValueBadge.tsx"
      provides: "Indigo badge with Sparkles icon"
      exports: ["BestValueBadge"]
    - path: "src/components/ai/DimensionScoreGrid.tsx"
      provides: "5-column grid of dimension scores"
      exports: ["DimensionScoreGrid"]
    - path: "src/components/ai/ExplainabilityPanel.tsx"
      provides: "AI narrative card with Bot icon"
      exports: ["ExplainabilityPanel"]
    - path: "src/components/ai/OverrideForm.tsx"
      provides: "Override form with justification textarea"
      exports: ["OverrideForm"]
  key_links:
    - from: "src/app/rfp/[id]/page.tsx"
      to: "src/lib/ai-engine.ts"
      via: "scoreBids and generateNarrative calls"
      pattern: "scoreBids"
    - from: "src/app/rfp/[id]/page.tsx"
      to: "src/context/ProcurementContext.tsx"
      via: "setEvaluation and setOverride actions"
      pattern: "setEvaluation"
    - from: "src/components/ai/ScoreRadarChart.tsx"
      to: "src/lib/mock-data/types.ts"
      via: "DimensionScores interface"
      pattern: "DimensionScores"
---

<objective>
Build the AI evaluation dashboard: trigger scoring, display ranked results with radar chart and score bars, show explainability narrative, and support manual override.

Purpose: This is the "wow moment" of Phase 2 -- the AI engine produces transparent, visual scoring that Gov Officers can understand and override. All components render inside the RFP detail page.
Output: 7 new AI components wired into the RFP detail page, enabling full evaluation-to-override workflow.
</objective>

<execution_context>
@/Users/ericorodrigues/Desktop/GSD_HackatonVGS/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ericorodrigues/Desktop/GSD_HackatonVGS/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-procurement-ai-engine/02-UI-SPEC.md
@.planning/phases/02-procurement-ai-engine/02-RESEARCH.md
@.planning/phases/02-procurement-ai-engine/02-01-SUMMARY.md
@.planning/phases/02-procurement-ai-engine/02-03-SUMMARY.md

<interfaces>
<!-- From src/lib/mock-data/types.ts (after Plan 01 extension): -->
```typescript
export interface DimensionScores {
  price: number;
  delivery: number;
  reliability: number;
  compliance: number;
  risk: number;
}

export interface ScoredBid {
  bid: Bid;
  supplier: Supplier;
  dimensions: DimensionScores;
  composite: number;
  rank: number;
  isWinner: boolean;
}
```

<!-- From src/lib/ai-engine.ts (created in Plan 01): -->
```typescript
export function scoreBids(bids: Bid[], suppliers: Supplier[], rfp: RFP): ScoredBid[];
export function generateNarrative(ranked: ScoredBid[]): string;
export const WEIGHTS: Record<string, number>;
```

<!-- From src/context/ProcurementContext.tsx (extended in Plan 01): -->
```typescript
export function useProcurement(): {
  suppliers: Supplier[];
  rfps: RFP[];
  addRFP: (rfp: RFP) => void;
  updateRFP: (id: string, updates: Partial<RFP>) => void;
  addBid: (rfpId: string, bid: Bid) => void;
  setEvaluation: (rfpId: string, results: ScoredBid[]) => void;
  setOverride: (rfpId: string, winnerId: string, justification: string) => void;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create all 7 AI dashboard components</name>
  <files>src/components/ai/ScoreRadarChart.tsx, src/components/ai/ScoreBar.tsx, src/components/ai/RankedSupplierRow.tsx, src/components/ai/BestValueBadge.tsx, src/components/ai/DimensionScoreGrid.tsx, src/components/ai/ExplainabilityPanel.tsx, src/components/ai/OverrideForm.tsx</files>
  <read_first>
    - src/lib/mock-data/types.ts (DimensionScores, ScoredBid interfaces)
    - src/lib/utils.ts (cn utility)
    - .planning/phases/02-procurement-ai-engine/02-UI-SPEC.md (exact visual specs for each component)
    - .planning/phases/02-procurement-ai-engine/02-RESEARCH.md (radar chart coordinate math, SVG approach)
  </read_first>
  <action>
1. **Create `src/components/ai/BestValueBadge.tsx`:**
   - Simple presentational component, no props needed (or optional className)
   - Import `Sparkles` from `lucide-react`
   - Render: `<span className="inline-flex items-center gap-1 bg-indigo-600 text-white px-3 py-1 rounded-full text-xs font-semibold" aria-label="AI recommended best value supplier"><Sparkles size={12} /> Best Value</span>`

2. **Create `src/components/ai/ScoreBar.tsx`:**
   - Props: `{ score: number; isWinner?: boolean; label?: string }`
   - Container: flex items-center gap-3
   - Label (if provided): `text-xs text-slate-500 w-20 text-right`
   - Track: `h-2 flex-1 rounded-full bg-slate-200` with `role="meter" aria-valuenow={score} aria-valuemin={0} aria-valuemax={100}`
   - Fill: `h-2 rounded-full transition-all duration-500` with width `${score}%`, color `bg-indigo-600` if isWinner, else `bg-slate-400`
   - Score number: `text-xs font-semibold text-slate-700 w-8`

3. **Create `src/components/ai/DimensionScoreGrid.tsx`:**
   - Props: `{ dimensions: DimensionScores }`
   - Import `DimensionScores` from `@/lib/mock-data/types`
   - Render grid: `grid grid-cols-5 gap-4`
   - For each dimension key in order `['price', 'delivery', 'reliability', 'compliance', 'risk']`:
     - Label: `text-xs text-slate-500 capitalize` showing dimension name
     - Score: `text-xl font-semibold text-slate-900` showing `dimensions[key]`

4. **Create `src/components/ai/ScoreRadarChart.tsx`:**
   - Props: `{ dimensions: DimensionScores; size?: number }`
   - Import `DimensionScores` from `@/lib/mock-data/types`
   - Constants: `SIZE = size || 280`, `CENTER = SIZE / 2`, `RADIUS = SIZE / 2 - 30`
   - AXES array: `['price', 'delivery', 'reliability', 'compliance', 'risk']`
   - Helper `getPoint(score: number, axisIndex: number)`:
     ```
     const angle = (axisIndex * 2 * Math.PI) / 5 - Math.PI / 2;
     const r = (score / 100) * RADIUS;
     return { x: CENTER + r * Math.cos(angle), y: CENTER + r * Math.sin(angle) };
     ```
   - Grid pentagons at 25%, 50%, 75%, 100%:
     - Each as `<polygon>` with points computed from `getPoint(percent, axisIndex)` for all 5 axes
     - `fill="none" stroke="#e2e8f0" strokeWidth="1"`
   - Axis lines from center to each 100% point:
     - `<line>` from CENTER,CENTER to getPoint(100, i), `stroke="#e2e8f0" strokeWidth="1"`
   - Axis labels at 110% radius points:
     - `<text>` at each axis endpoint (slightly beyond), `fontSize="11" fill="#64748b" textAnchor="middle" dominantBaseline="middle"` showing capitalized dimension name
   - Data polygon: connect getPoint(dimensions[axis], axisIndex) for all 5 axes
     - `fill="rgba(79, 70, 229, 0.2)" stroke="#4f46e5" strokeWidth="2"`
   - Dot on each data point: `<circle>` r="3" `fill="#4f46e5"`
   - Visually hidden data table for screen readers: `<div className="sr-only"><table>...</table></div>` listing each dimension and score

5. **Create `src/components/ai/RankedSupplierRow.tsx`:**
   - Props: `{ scoredBid: ScoredBid; onSelect?: () => void }`
   - Import `ScoredBid` from `@/lib/mock-data/types`, `BestValueBadge`, `DimensionScoreGrid`, `ScoreBar`, `cn` from `@/lib/utils`
   - Container: `bg-white rounded-xl border p-4 hover:bg-slate-50 transition-colors` with conditional left border: `border-l-4 border-l-indigo-600` if scoredBid.isWinner, else `border-l-4 border-l-transparent`
   - Top row: flex justify-between items-center
     - Left: rank number (`#${scoredBid.rank}` in `text-sm text-slate-400 font-mono`), supplier name (`text-lg font-semibold text-slate-900`), BestValueBadge if isWinner
     - Right: composite score in `text-3xl font-bold text-slate-900` with `/100` in `text-sm text-slate-400`
   - Below: DimensionScoreGrid showing scoredBid.dimensions
   - Below that: individual ScoreBars for each dimension, with `isWinner={scoredBid.isWinner}` and `label` set to dimension name

6. **Create `src/components/ai/ExplainabilityPanel.tsx`:**
   - Props: `{ narrative: string }`
   - Import `Bot` from `lucide-react`
   - Container: `bg-indigo-50 border border-indigo-200 rounded-xl p-6`
   - Header row: flex items-center gap-2
     - `<Bot size={20} className="text-indigo-600" />`
     - `<span className="text-sm font-semibold text-indigo-900">AI Analysis</span>`
   - Narrative: `<p className="mt-3 text-sm text-slate-700 leading-relaxed">{narrative}</p>`

7. **Create `src/components/ai/OverrideForm.tsx`:**
   - Props: `{ scoredBids: ScoredBid[]; currentWinnerId: string; onOverride: (winnerId: string, justification: string) => void }`
   - Import `useState` from `react`, `ScoredBid` from `@/lib/mock-data/types`, `AlertTriangle` from `lucide-react`
   - State: `{ isExpanded: false, selectedSupplierId: '', justification: '' }`
   - Toggle button: "Override Recommendation" in `text-sm text-amber-700 hover:text-amber-800 font-medium` with AlertTriangle icon
   - When expanded, show `bg-amber-50 border border-amber-200 rounded-xl p-6 mt-4`:
     - Warning text: "You are overriding the AI's best-value recommendation. This action will be logged for audit."
     - Supplier select: `<select>` listing scoredBids that are NOT the current winner. Options: `${scoredBid.supplier.name} (Score: ${scoredBid.composite}/100)`
     - Justification textarea: required, `min 20 characters`, placeholder "Provide a written justification for this override (minimum 20 characters)..."
     - Character count below textarea: `text-xs text-slate-400` showing `${justification.length}/20 minimum`
     - Confirm button: disabled if !selectedSupplierId or justification.length < 20. `bg-amber-600 hover:bg-amber-700 text-white px-4 py-2 rounded-lg text-sm font-semibold disabled:opacity-50 disabled:cursor-not-allowed`
     - On confirm, call `onOverride(selectedSupplierId, justification)`
  </action>
  <verify>
    <automated>cd /Users/ericorodrigues/Desktop/GSD_HackatonVGS && npx tsc --noEmit src/components/ai/ScoreRadarChart.tsx src/components/ai/ScoreBar.tsx src/components/ai/RankedSupplierRow.tsx src/components/ai/BestValueBadge.tsx src/components/ai/DimensionScoreGrid.tsx src/components/ai/ExplainabilityPanel.tsx src/components/ai/OverrideForm.tsx 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/components/ai/BestValueBadge.tsx contains `Best Value` and `Sparkles`
    - src/components/ai/BestValueBadge.tsx contains `aria-label`
    - src/components/ai/ScoreBar.tsx contains `role="meter"` and `aria-valuenow`
    - src/components/ai/ScoreBar.tsx contains `bg-indigo-600` (winner color)
    - src/components/ai/DimensionScoreGrid.tsx contains `grid grid-cols-5`
    - src/components/ai/ScoreRadarChart.tsx contains `<svg`
    - src/components/ai/ScoreRadarChart.tsx contains `polygon` (data polygon)
    - src/components/ai/ScoreRadarChart.tsx contains `Math.cos` and `Math.sin` (coordinate calc)
    - src/components/ai/ScoreRadarChart.tsx contains `sr-only` (screen reader table)
    - src/components/ai/RankedSupplierRow.tsx contains `border-l-indigo-600` (winner indicator)
    - src/components/ai/RankedSupplierRow.tsx contains `BestValueBadge`
    - src/components/ai/ExplainabilityPanel.tsx contains `Bot` and `AI Analysis`
    - src/components/ai/ExplainabilityPanel.tsx contains `bg-indigo-50`
    - src/components/ai/OverrideForm.tsx contains `Override Recommendation`
    - src/components/ai/OverrideForm.tsx contains `bg-amber-50` (warning styling)
    - src/components/ai/OverrideForm.tsx contains `justification.length < 20` (validation)
  </acceptance_criteria>
  <done>All 7 AI dashboard components created: BestValueBadge, ScoreBar, DimensionScoreGrid, ScoreRadarChart (SVG), RankedSupplierRow, ExplainabilityPanel, OverrideForm. All type-check clean and follow UI-SPEC visual specs.</done>
</task>

<task type="auto">
  <name>Task 2: Wire AI evaluation into RFP detail page</name>
  <files>src/app/rfp/[id]/page.tsx</files>
  <read_first>
    - src/app/rfp/[id]/page.tsx (current RFP detail page from Plan 03)
    - src/lib/ai-engine.ts (scoreBids, generateNarrative functions)
    - src/context/ProcurementContext.tsx (setEvaluation, setOverride, updateRFP)
    - src/components/ai/ScoreRadarChart.tsx (from Task 1)
    - src/components/ai/RankedSupplierRow.tsx (from Task 1)
    - src/components/ai/ExplainabilityPanel.tsx (from Task 1)
    - src/components/ai/OverrideForm.tsx (from Task 1)
    - src/lib/mock-data/types.ts (ScoredBid, DimensionScores)
    - .planning/phases/02-procurement-ai-engine/02-UI-SPEC.md (AI evaluation flow, interaction contract)
  </read_first>
  <action>
Extend the existing `src/app/rfp/[id]/page.tsx` (from Plan 03) by adding the AI evaluation section below the bids table. Preserve ALL existing page content (heading, timeline, details, bids table, publish button).

1. Add new imports:
   - `scoreBids, generateNarrative` from `@/lib/ai-engine`
   - `RankedSupplierRow` from `@/components/ai/RankedSupplierRow`
   - `ScoreRadarChart` from `@/components/ai/ScoreRadarChart`
   - `ExplainabilityPanel` from `@/components/ai/ExplainabilityPanel`
   - `OverrideForm` from `@/components/ai/OverrideForm`
   - `Bot`, `Loader2` from `lucide-react`
   - `toast` from `react-hot-toast`

2. Add state:
   - `const [isEvaluating, setIsEvaluating] = useState(false)`

3. Add `setEvaluation`, `setOverride` from `useProcurement()` destructuring.

4. After the bids section, add AI Evaluation section (only visible when role === 'gov'):

   **"Run AI Evaluation" button** (shown when rfp.status === 'Open' and rfp.bids.length >= 2 and no evaluationResults yet):
   - `<button onClick={handleEvaluate} disabled={rfp.bids.length < 2 || isEvaluating} className="bg-indigo-600 hover:bg-indigo-700 text-white px-6 py-2.5 rounded-lg text-sm font-semibold inline-flex items-center gap-2 disabled:opacity-50 disabled:cursor-not-allowed">`
   - Icon: `<Bot size={16} />`
   - Text: isEvaluating ? "Analyzing bids..." : "Run AI Evaluation"
   - If isEvaluating, show Loader2 icon with `animate-spin` class instead of Bot

   **handleEvaluate function:**
   ```
   setIsEvaluating(true);
   // Simulate 1.5s AI processing
   setTimeout(() => {
     const results = scoreBids(rfp.bids, suppliers, rfp);
     setEvaluation(rfp.id, results);
     updateRFP(rfp.id, { status: 'Evaluating' });
     setIsEvaluating(false);
     toast.success('AI evaluation complete');
   }, 1500);
   ```

   **If rfp.bids.length < 2** (not enough bids): show empty state card with `bg-slate-50 rounded-xl p-6 text-center`:
   - Heading: "Awaiting evaluation"
   - Body: "At least 2 bids are required before running AI evaluation."

5. **AI Results section** (shown when rfp.evaluationResults exists and has length > 0):
   - Section heading: "AI Evaluation Results" with Bot icon, `text-xl font-semibold text-slate-900`

   - Determine effective winner: if rfp.overrideWinnerId, find the overridden winner in evaluationResults. Otherwise, the evaluationResults[0] (rank 1) is the winner.

   - **Ranked list**: Map evaluationResults to `<RankedSupplierRow>` components. For each scoredBid, pass `scoredBid` prop. If override exists, mutate display: the overridden supplier's row gets `isWinner` visual treatment (left border), original winner loses it. Show "Manually Selected" badge (amber) on override winner instead of BestValueBadge.

   - **Radar chart for winner**: `<ScoreRadarChart dimensions={winnerScoredBid.dimensions} />` in a white card

   - **ExplainabilityPanel**: `<ExplainabilityPanel narrative={generateNarrative(rfp.evaluationResults)} />`

   - **"Award Supplier" button** (shown when status is 'Evaluating' and no override or after override):
     - `bg-indigo-600 hover:bg-indigo-700 text-white px-6 py-2.5 rounded-lg text-sm font-semibold`
     - Text: "Award Supplier"
     - onClick: `updateRFP(rfp.id, { status: 'Awarded', selectedWinnerId: effectiveWinnerId }); toast.success('Supplier awarded');`

   - **OverrideForm** (shown when status is 'Evaluating' and no override yet, gov role only):
     - `<OverrideForm scoredBids={rfp.evaluationResults} currentWinnerId={rfp.evaluationResults[0].supplier.id} onOverride={(winnerId, justification) => { setOverride(rfp.id, winnerId, justification); toast.success('Override applied'); }} />`
  </action>
  <verify>
    <automated>cd /Users/ericorodrigues/Desktop/GSD_HackatonVGS && npx tsc --noEmit src/app/rfp/\\[id\\]/page.tsx 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/app/rfp/[id]/page.tsx contains `import { scoreBids, generateNarrative }` from ai-engine
    - src/app/rfp/[id]/page.tsx contains `Run AI Evaluation` (button text)
    - src/app/rfp/[id]/page.tsx contains `setEvaluation` (context action)
    - src/app/rfp/[id]/page.tsx contains `setTimeout` (1.5s simulated processing)
    - src/app/rfp/[id]/page.tsx contains `RankedSupplierRow`
    - src/app/rfp/[id]/page.tsx contains `ScoreRadarChart`
    - src/app/rfp/[id]/page.tsx contains `ExplainabilityPanel`
    - src/app/rfp/[id]/page.tsx contains `OverrideForm`
    - src/app/rfp/[id]/page.tsx contains `Award Supplier` (award button text)
    - src/app/rfp/[id]/page.tsx contains `Analyzing bids` (loading state text)
    - src/app/rfp/[id]/page.tsx contains `Awaiting evaluation` (empty state for < 2 bids)
    - src/app/rfp/[id]/page.tsx contains `Manually Selected` (override badge)
    - src/app/rfp/[id]/page.tsx contains `setOverride` (override context action)
    - src/app/rfp/[id]/page.tsx still contains `RFPStatusTimeline` (preserved from Plan 03)
    - src/app/rfp/[id]/page.tsx still contains `Publish RFP` (preserved from Plan 03)
  </acceptance_criteria>
  <done>RFP detail page has full AI evaluation workflow: "Run AI Evaluation" button triggers 1.5s loading then shows ranked results with radar chart, score bars, dimension grid, explainability narrative, override form, and "Award Supplier" button. All Plan 03 content preserved.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes for all AI component files and the RFP detail page
- "Run AI Evaluation" button appears on RFP detail when >= 2 bids exist
- Clicking evaluation shows loading state, then ranked results
- Winner has "Best Value" badge and indigo left border
- Radar chart renders 5-axis SVG polygon
- Explainability panel shows narrative with supplier names and scores
- Override form expands, requires 20-char justification, swaps winner visual
- "Award Supplier" button updates RFP status to Awarded
</verification>

<success_criteria>
- Full AI evaluation flow works: trigger -> loading -> ranked results with all visual components
- Radar chart, score bars, dimension grid, and explainability panel all render correctly
- Manual override swaps winner indicator and preserves original recommendation
- Award button transitions RFP to Awarded status
- All Phase 2 success criteria achievable through this workflow
</success_criteria>

<output>
After completion, create `.planning/phases/02-procurement-ai-engine/02-04-SUMMARY.md`
</output>
