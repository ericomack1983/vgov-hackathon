---
phase: 02-procurement-ai-engine
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/rfp/page.tsx
  - src/app/rfp/new/page.tsx
  - src/app/rfp/[id]/page.tsx
  - src/app/bids/page.tsx
  - src/components/procurement/CreateRFPForm.tsx
  - src/components/procurement/RFPStatusTimeline.tsx
  - src/components/procurement/BidFormModal.tsx
autonomous: true
requirements: [PROC-01, PROC-02, PROC-03, SUPP-03]

must_haves:
  truths:
    - "Gov Officer can create a new RFP with title, description, budget ceiling, deadline, and category -- it appears in the RFP list with correct status"
    - "RFP list page shows all RFPs with status badges (Draft, Open, Evaluating, Awarded, Paid)"
    - "RFP detail page shows description, submitted bids, and status timeline"
    - "Supplier can submit a bid on an open RFP with bid amount, delivery days, and notes"
  artifacts:
    - path: "src/app/rfp/page.tsx"
      provides: "RFP list with status badges and Create RFP button"
      contains: "useProcurement"
    - path: "src/app/rfp/new/page.tsx"
      provides: "Create RFP form page"
      contains: "CreateRFPForm"
    - path: "src/app/rfp/[id]/page.tsx"
      provides: "RFP detail with bids table and status timeline"
      contains: "use(params)"
    - path: "src/app/bids/page.tsx"
      provides: "Supplier bid list with bid submission modal"
      contains: "BidFormModal"
    - path: "src/components/procurement/CreateRFPForm.tsx"
      provides: "5-field form for RFP creation"
      exports: ["CreateRFPForm"]
    - path: "src/components/procurement/RFPStatusTimeline.tsx"
      provides: "Horizontal 5-step status timeline"
      exports: ["RFPStatusTimeline"]
    - path: "src/components/procurement/BidFormModal.tsx"
      provides: "Modal with 3 fields for bid submission"
      exports: ["BidFormModal"]
  key_links:
    - from: "src/app/rfp/new/page.tsx"
      to: "src/context/ProcurementContext.tsx"
      via: "addRFP action"
      pattern: "addRFP"
    - from: "src/app/bids/page.tsx"
      to: "src/context/ProcurementContext.tsx"
      via: "addBid action"
      pattern: "addBid"
    - from: "src/app/rfp/[id]/page.tsx"
      to: "src/context/ProcurementContext.tsx"
      via: "rfps.find and updateRFP"
      pattern: "rfps.find"
---

<objective>
Build the complete RFP workflow: list page, create form, detail page with bid table and status timeline, and supplier bid submission page with modal.

Purpose: This is the core procurement workflow that feeds into AI evaluation. Gov Officer creates RFPs, suppliers submit bids, and the detail page shows all bids ready for evaluation.
Output: Working /rfp, /rfp/new, /rfp/[id], and /bids pages with full CRUD operations via ProcurementContext.
</objective>

<execution_context>
@/Users/ericorodrigues/Desktop/GSD_HackatonVGS/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ericorodrigues/Desktop/GSD_HackatonVGS/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-procurement-ai-engine/02-UI-SPEC.md
@.planning/phases/02-procurement-ai-engine/02-RESEARCH.md

<interfaces>
<!-- From src/lib/mock-data/types.ts: -->
```typescript
export type RFPStatus = 'Draft' | 'Open' | 'Evaluating' | 'Awarded' | 'Paid';

export interface Bid {
  id: string;
  rfpId: string;
  supplierId: string;
  supplierName: string;
  amount: number;
  deliveryDays: number;
  notes: string;
  submittedAt: string;
}

export interface RFP {
  id: string;
  title: string;
  description: string;
  budgetCeiling: number;
  deadline: string;
  category: string;
  status: RFPStatus;
  createdAt: string;
  bids: Bid[];
  selectedWinnerId?: string;
  evaluationResults?: ScoredBid[];
  overrideWinnerId?: string;
  overrideJustification?: string;
}
```

<!-- From src/context/ProcurementContext.tsx: -->
```typescript
export function useProcurement(): {
  suppliers: Supplier[];
  rfps: RFP[];
  addRFP: (rfp: RFP) => void;
  updateRFP: (id: string, updates: Partial<RFP>) => void;
  addBid: (rfpId: string, bid: Bid) => void;
}
```

<!-- From src/context/UIContext.tsx: -->
```typescript
export function useUI(): {
  role: Role; // 'gov' | 'supplier' | 'auditor'
  setRole: (role: Role) => void;
  sidebarOpen: boolean;
  setSidebarOpen: (open: boolean) => void;
}
```

<!-- From src/components/shared/StatusBadge.tsx: -->
```typescript
export function StatusBadge({ status, variant }: {
  status: string;
  variant?: 'default' | 'success' | 'warning' | 'error';
}): JSX.Element
```

<!-- 5 mock RFPs exist with statuses: Open, Evaluating, Awarded, Draft, Paid -->
<!-- RFP categories in mock data: 'IT Infrastructure', 'Cybersecurity', 'Office Supplies', 'Data & Analytics', 'Facilities' -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create RFP components (CreateRFPForm, RFPStatusTimeline, BidFormModal)</name>
  <files>src/components/procurement/CreateRFPForm.tsx, src/components/procurement/RFPStatusTimeline.tsx, src/components/procurement/BidFormModal.tsx</files>
  <read_first>
    - src/lib/mock-data/types.ts (RFP, Bid, RFPStatus types)
    - src/context/ProcurementContext.tsx (addRFP, addBid actions)
    - src/context/UIContext.tsx (useUI for role checking)
    - src/components/shared/StatusBadge.tsx (reuse pattern)
    - src/lib/utils.ts (cn utility)
    - .planning/phases/02-procurement-ai-engine/02-UI-SPEC.md (component specs, interaction contracts, copywriting)
  </read_first>
  <action>
1. **Create `src/components/procurement/RFPStatusTimeline.tsx`:**
   - `'use client'` directive
   - Props: `{ currentStatus: RFPStatus }`
   - Import `cn` from `@/lib/utils`, `Check` from `lucide-react`
   - Define steps array: `['Draft', 'Open', 'Evaluating', 'Awarded', 'Paid']`
   - Find current step index by matching currentStatus
   - Render horizontal flex row with each step:
     - Circle: 32x32 (w-8 h-8) rounded-full flex items-center justify-center
     - Completed step (index < currentIndex): `bg-emerald-500 text-white` with Check icon (size 16)
     - Active step (index === currentIndex): `bg-indigo-600 text-white` with step number
     - Future step (index > currentIndex): `bg-slate-200 text-slate-400` with step number
     - Label below circle: `text-xs mt-1` with `text-indigo-600 font-semibold` for active, `text-emerald-600` for completed, `text-slate-400` for future
     - Connecting line between circles: `h-0.5 flex-1 mx-2` with `bg-emerald-500` if next step is completed/active, else `bg-slate-200`
   - Export `RFPStatusTimeline`

2. **Create `src/components/procurement/CreateRFPForm.tsx`:**
   - `'use client'` directive
   - Props: none (self-contained, uses context)
   - Import `useState` from `react`, `useProcurement` from `@/context/ProcurementContext`, `useRouter` from `next/navigation`, `v4 as uuidv4` from `uuid`, `toast` from `react-hot-toast`
   - State: `{ title: '', description: '', budgetCeiling: '', deadline: '', category: '' }` and `errors: Record<string, string>`
   - Categories: `['IT Infrastructure', 'Cybersecurity', 'Office Supplies', 'Data & Analytics', 'Facilities', 'Professional Services', 'Construction']`
   - Validation on submit:
     - title: required (error: "Title is required")
     - description: required (error: "Description is required")
     - budgetCeiling: must be > 0 (error: "Budget must be greater than 0")
     - deadline: required and must be in the future (error: "Deadline must be a future date")
     - category: required (error: "Category is required")
   - If validation passes:
     - Create RFP object: `{ id: uuidv4(), title, description, budgetCeiling: Number(budgetCeiling), deadline: new Date(deadline).toISOString(), category, status: 'Draft' as const, createdAt: new Date().toISOString(), bids: [] }`
     - Call `addRFP(rfp)`
     - `toast.success('RFP created successfully')`
     - `router.push('/rfp/' + rfp.id)`
   - Form layout: white card (bg-white rounded-xl shadow-sm border border-slate-200 p-6)
   - Each field: `<label className="block text-sm font-semibold text-slate-700 mb-1">` + input with `w-full rounded-lg border border-slate-300 px-3 py-2 text-sm focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500`
   - Error display below each field: `<p className="mt-1 text-xs text-red-500">{error}</p>`
   - Title: `<input type="text">`
   - Description: `<textarea rows={4}>`
   - Budget Ceiling: `<input type="number" min="0" step="1000">`
   - Deadline: `<input type="date">`
   - Category: `<select>` with `<option value="">Select a category</option>` + mapped categories
   - Submit button: `bg-indigo-600 hover:bg-indigo-700 text-white px-6 py-2 rounded-lg text-sm font-semibold transition-colors` with text "Create RFP"
   - Export `CreateRFPForm`

3. **Create `src/components/procurement/BidFormModal.tsx`:**
   - `'use client'` directive
   - Props: `{ rfpId: string; rfpTitle: string; isOpen: boolean; onClose: () => void }`
   - Import `useState`, `useEffect`, `useCallback` from `react`, `useProcurement` from `@/context/ProcurementContext`, `useUI` from `@/context/UIContext`, `v4 as uuidv4` from `uuid`, `toast` from `react-hot-toast`, `X` from `lucide-react`
   - If `!isOpen` return null
   - State: `{ amount: '', deliveryDays: '', notes: '' }` and `errors: Record<string, string>`
   - Escape key handler: `useEffect` with `onKeyDown` listener that calls `onClose` on Escape key. Cleanup on unmount.
   - Focus trap: `useEffect` that focuses the first input on mount.
   - Backdrop click: `onClick` on overlay calls `onClose`
   - Content click: `onClick` with `e.stopPropagation()` to prevent closing
   - Validation on submit:
     - amount: must be > 0 (error: "Bid amount must be greater than 0")
     - deliveryDays: must be > 0 (error: "Delivery days must be greater than 0")
   - On submit:
     - Get role-based supplier info. For demo, use first supplier's data or map from role. Since mock, use: `supplierId: 'sup-002', supplierName: 'BudgetGov Supplies'` (this is the demo supplier role). Actually, let the user pick -- but for simplicity in hackathon, hardcode a demo supplier. Use `supplierId: 'sup-demo', supplierName: 'Demo Supplier'` with a note this represents the logged-in supplier.
     - Create bid: `{ id: uuidv4(), rfpId, supplierId: 'sup-demo', supplierName: 'Demo Supplier', amount: Number(amount), deliveryDays: Number(deliveryDays), notes, submittedAt: new Date().toISOString() }`
     - Call `addBid(rfpId, bid)`
     - `toast.success('Bid submitted successfully')`
     - Call `onClose()`
     - Reset form
   - Overlay: `fixed inset-0 z-50 flex items-center justify-center bg-black/50`
   - Modal card: `bg-white rounded-xl shadow-xl max-w-lg w-full mx-4 p-6`
   - Header: flex justify-between items-center. Title "Submit Bid" in `text-lg font-semibold text-slate-900`. Close button with X icon.
   - Subtitle: `<p className="text-sm text-slate-500 mt-1">Bidding on: {rfpTitle}</p>`
   - Fields same styling as CreateRFPForm:
     - Bid Amount ($): `<input type="number" min="0" step="100">`
     - Delivery Days: `<input type="number" min="1">`
     - Notes: `<textarea rows={3}>`
   - Buttons: Cancel (text-slate-600 hover:text-slate-800) + Submit Bid (bg-indigo-600 hover:bg-indigo-700 text-white px-6 py-2 rounded-lg text-sm font-semibold)
   - Export `BidFormModal`
  </action>
  <verify>
    <automated>cd /Users/ericorodrigues/Desktop/GSD_HackatonVGS && npx tsc --noEmit src/components/procurement/CreateRFPForm.tsx src/components/procurement/RFPStatusTimeline.tsx src/components/procurement/BidFormModal.tsx 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/components/procurement/CreateRFPForm.tsx contains `export function CreateRFPForm`
    - src/components/procurement/CreateRFPForm.tsx contains `addRFP` (context action call)
    - src/components/procurement/CreateRFPForm.tsx contains `uuidv4` (ID generation)
    - src/components/procurement/CreateRFPForm.tsx contains `router.push` (redirect after create)
    - src/components/procurement/CreateRFPForm.tsx contains `toast.success` (success feedback)
    - src/components/procurement/RFPStatusTimeline.tsx contains `export function RFPStatusTimeline`
    - src/components/procurement/RFPStatusTimeline.tsx contains `Draft` and `Open` and `Evaluating` and `Awarded` and `Paid`
    - src/components/procurement/BidFormModal.tsx contains `export function BidFormModal`
    - src/components/procurement/BidFormModal.tsx contains `addBid` (context action call)
    - src/components/procurement/BidFormModal.tsx contains `Escape` (keyboard handler)
    - src/components/procurement/BidFormModal.tsx contains `onClose`
    - src/components/procurement/BidFormModal.tsx contains `bg-black/50` (overlay)
  </acceptance_criteria>
  <done>Three procurement components ready: CreateRFPForm with validation and 5 fields, RFPStatusTimeline with 5-step horizontal timeline, BidFormModal with 3 fields, escape key, and overlay dismiss.</done>
</task>

<task type="auto">
  <name>Task 2: Build RFP list, create, detail pages and bids page</name>
  <files>src/app/rfp/page.tsx, src/app/rfp/new/page.tsx, src/app/rfp/[id]/page.tsx, src/app/bids/page.tsx</files>
  <read_first>
    - src/app/rfp/page.tsx (current placeholder to replace)
    - src/app/bids/page.tsx (current placeholder to replace)
    - src/components/procurement/CreateRFPForm.tsx (from Task 1)
    - src/components/procurement/RFPStatusTimeline.tsx (from Task 1)
    - src/components/procurement/BidFormModal.tsx (from Task 1)
    - src/components/shared/StatusBadge.tsx (for status display)
    - src/components/shared/PlaceholderPage.tsx (motion pattern)
    - src/context/ProcurementContext.tsx (useProcurement hook)
    - src/context/UIContext.tsx (useUI for role-gating)
    - src/lib/mock-data/rfps.ts (mock data structure)
    - .planning/phases/02-procurement-ai-engine/02-UI-SPEC.md (page specs, interaction contracts, copywriting)
    - .planning/phases/02-procurement-ai-engine/02-RESEARCH.md (Next.js 16 use(params) pattern)
  </read_first>
  <action>
1. **Replace `src/app/rfp/page.tsx` (RFP List):**
   - `'use client'` directive
   - Import `motion` from `framer-motion`, `useProcurement`, `useUI`, `StatusBadge`, `Link` from `next/link`, `Plus` from `lucide-react`, `format` from `date-fns`
   - Page heading: "Procurement Requests" + subtitle "Create and track RFPs across all stages."
   - If role === 'gov': show "Create RFP" button (Link to /rfp/new) with Plus icon, `bg-indigo-600 hover:bg-indigo-700 text-white px-4 py-2 rounded-lg text-sm font-semibold inline-flex items-center gap-2`
   - Table container: `bg-white rounded-xl shadow-sm border border-slate-200 overflow-hidden mt-6`
   - Table with columns: Title, Category, Budget, Deadline, Status, Bids
   - Table header: `bg-slate-50 text-xs font-semibold text-slate-500 uppercase tracking-wider`
   - Table rows: map rfps, each row clickable via `<Link href={/rfp/${rfp.id}}>`
   - Status badge variant mapping: Draft -> default, Open -> warning, Evaluating -> warning, Awarded -> success, Paid -> success
   - Budget formatted: `$${rfp.budgetCeiling.toLocaleString()}`
   - Deadline formatted: `format(new Date(rfp.deadline), 'MMM d, yyyy')`
   - Bids count: `rfp.bids.length`
   - Empty state: heading "No procurement requests yet", body "Create your first RFP to start the procurement process."
   - Wrap in motion.div with standard animation

2. **Create `src/app/rfp/new/page.tsx` (Create RFP):**
   - `'use client'` directive
   - Import `motion` from `framer-motion`, `CreateRFPForm`, `Link` from `next/link`, `ArrowLeft` from `lucide-react`, `useUI` from `@/context/UIContext`
   - Role gate: if role !== 'gov', show "Access Denied" message with link back to /rfp
   - Back link: `<Link href="/rfp">` with ArrowLeft + "Back to RFPs"
   - Page heading: "Create New RFP"
   - Render `<CreateRFPForm />`
   - Wrap in motion.div

3. **Create `src/app/rfp/[id]/page.tsx` (RFP Detail):**
   - `'use client'` directive
   - Import `use`, `useState` from `react`, `motion` from `framer-motion`, `useProcurement`, `useUI`, `RFPStatusTimeline`, `StatusBadge`, `Link` from `next/link`, `ArrowLeft`, `FileText` from `lucide-react`, `format` from `date-fns`
   - Next.js 16 params pattern: `const { id } = use(params)`
   - Find RFP: `rfps.find(r => r.id === id)`
   - Not found state: "RFP not found" with back link
   - Back link to /rfp
   - Page heading: rfp.title + status badge
   - RFPStatusTimeline component below heading
   - Details card (white, rounded-xl, p-6):
     - Description section: `text-sm text-slate-700` rendering rfp.description
     - Info grid (grid-cols-2 md:grid-cols-4 gap-4 mt-4):
       - Category, Budget ($formatted), Deadline (formatted), Created (formatted)
   - If role === 'gov' and rfp.status === 'Draft': "Publish RFP" button that calls `updateRFP(rfp.id, { status: 'Open' })`, styled as `bg-indigo-600 hover:bg-indigo-700 text-white px-4 py-2 rounded-lg text-sm font-semibold`
   - Bids section (mt-6):
     - Heading "Submitted Bids" with count badge
     - If bids.length === 0: "No bids submitted yet"
     - Else: table with columns: Supplier, Amount, Delivery, Notes, Submitted
     - Amount: `$${bid.amount.toLocaleString()}`
     - Delivery: `${bid.deliveryDays} days`
     - Submitted: `format(new Date(bid.submittedAt), 'MMM d, yyyy')`
   - NOTE: Do NOT add AI evaluation UI here -- Plan 04 will add that section. This page only shows basic RFP info + bids.

4. **Replace `src/app/bids/page.tsx` (Supplier Bids):**
   - `'use client'` directive
   - Import `useState` from `react`, `motion` from `framer-motion`, `useProcurement`, `useUI`, `BidFormModal`, `StatusBadge`, `FileText` from `lucide-react`
   - Page heading: "Submit Bids" + subtitle "Browse open procurement requests and submit your bids."
   - Filter RFPs to only show status === 'Open': `const openRfps = rfps.filter(r => r.status === 'Open')`
   - State: `selectedRfp: { id: string; title: string } | null` for modal
   - List of open RFPs as cards (not table):
     - Each card: white, rounded-xl, border, p-4 with rfp.title (font-semibold), category badge, budget, deadline
     - "Submit Bid" button on each card: `bg-indigo-600 hover:bg-indigo-700 text-white px-4 py-2 rounded-lg text-sm font-semibold`
     - onClick sets selectedRfp to open modal
   - `<BidFormModal isOpen={!!selectedRfp} rfpId={selectedRfp?.id || ''} rfpTitle={selectedRfp?.title || ''} onClose={() => setSelectedRfp(null)} />`
   - Empty state: heading "No open RFPs available", body "There are no open procurement requests to bid on right now."
   - Wrap in motion.div
  </action>
  <verify>
    <automated>cd /Users/ericorodrigues/Desktop/GSD_HackatonVGS && npx tsc --noEmit src/app/rfp/page.tsx src/app/rfp/new/page.tsx src/app/rfp/\\[id\\]/page.tsx src/app/bids/page.tsx 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - src/app/rfp/page.tsx does NOT contain `PlaceholderPage`
    - src/app/rfp/page.tsx contains `useProcurement`
    - src/app/rfp/page.tsx contains `Create RFP` (CTA text)
    - src/app/rfp/page.tsx contains `StatusBadge`
    - src/app/rfp/page.tsx contains `No procurement requests yet` (empty state)
    - src/app/rfp/new/page.tsx contains `CreateRFPForm`
    - src/app/rfp/new/page.tsx contains `role !== 'gov'` or equivalent role gate
    - src/app/rfp/[id]/page.tsx contains `use(params)` (Next.js 16 pattern)
    - src/app/rfp/[id]/page.tsx contains `params: Promise<{ id: string }>`
    - src/app/rfp/[id]/page.tsx contains `RFPStatusTimeline`
    - src/app/rfp/[id]/page.tsx contains `updateRFP` (Publish button)
    - src/app/rfp/[id]/page.tsx contains `Publish RFP`
    - src/app/bids/page.tsx does NOT contain `PlaceholderPage`
    - src/app/bids/page.tsx contains `BidFormModal`
    - src/app/bids/page.tsx contains `Submit Bid`
    - src/app/bids/page.tsx contains `No open RFPs available` (empty state)
  </acceptance_criteria>
  <done>RFP list shows all 5 mock RFPs with status badges. Create RFP form adds new RFPs via context. RFP detail shows description, bids table, and status timeline with Publish button. Bids page lists open RFPs with bid submission modal.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes for all 7 files
- /rfp page shows 5 RFPs with correct status badges
- /rfp/new shows form with 5 fields (gov role only)
- /rfp/[id] shows detail with timeline, description, and bids table
- /bids page shows open RFPs with "Submit Bid" triggering modal
- Creating an RFP redirects to its detail page
- Submitting a bid adds it to the RFP context
</verification>

<success_criteria>
- Gov Officer can create RFP with all 5 fields, it appears in list with Draft status
- RFP list shows all requests with proper status badges and budget formatting
- RFP detail shows status timeline, description, and submitted bids
- Gov Officer can publish a Draft RFP (changes to Open)
- Supplier can submit bid via modal on open RFP
- All pages use consistent motion animations and styling
</success_criteria>

<output>
After completion, create `.planning/phases/02-procurement-ai-engine/02-03-SUMMARY.md`
</output>
