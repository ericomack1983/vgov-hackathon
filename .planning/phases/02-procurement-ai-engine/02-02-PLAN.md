---
phase: 02-procurement-ai-engine
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/suppliers/page.tsx
  - src/app/suppliers/[id]/page.tsx
  - src/components/procurement/SupplierCard.tsx
autonomous: true
requirements: [SUPP-01, SUPP-02]

must_haves:
  truths:
    - "Supplier registry page lists all 8 suppliers with name, rating, compliance status, and certifications"
    - "Clicking a supplier card navigates to /suppliers/[id] profile page"
    - "Supplier profile shows rating, past performance, pricing history sparkline, wallet address, and compliance badge"
  artifacts:
    - path: "src/app/suppliers/page.tsx"
      provides: "Supplier registry grid page"
      contains: "useProcurement"
    - path: "src/app/suppliers/[id]/page.tsx"
      provides: "Supplier profile detail page"
      contains: "use(params)"
    - path: "src/components/procurement/SupplierCard.tsx"
      provides: "Grid card component for registry"
      exports: ["SupplierCard"]
  key_links:
    - from: "src/app/suppliers/page.tsx"
      to: "src/context/ProcurementContext.tsx"
      via: "useProcurement().suppliers"
      pattern: "useProcurement"
    - from: "src/app/suppliers/[id]/page.tsx"
      to: "src/context/ProcurementContext.tsx"
      via: "useProcurement().suppliers.find"
      pattern: "suppliers.find"
---

<objective>
Build the supplier registry grid page and supplier profile detail page, replacing the placeholder.

Purpose: Gov Officers and Auditors need to browse and inspect supplier details before procurement evaluation.
Output: Real /suppliers page with grid of SupplierCards, /suppliers/[id] profile page with full supplier details.
</objective>

<execution_context>
@/Users/ericorodrigues/Desktop/GSD_HackatonVGS/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ericorodrigues/Desktop/GSD_HackatonVGS/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-procurement-ai-engine/02-UI-SPEC.md
@.planning/phases/02-procurement-ai-engine/02-RESEARCH.md

<interfaces>
<!-- From src/lib/mock-data/types.ts: -->
```typescript
export interface Supplier {
  id: string;
  name: string;
  rating: number;
  complianceStatus: 'Compliant' | 'Pending Review' | 'Non-Compliant';
  certifications: string[];
  pastPerformance: number;
  pricingHistory: number[];
  walletAddress: string;
  deliveryAvgDays: number;
  riskScore: number;
}
```

<!-- From src/context/ProcurementContext.tsx: -->
```typescript
export function useProcurement(): {
  suppliers: Supplier[];
  rfps: RFP[];
  addRFP: (rfp: RFP) => void;
  updateRFP: (id: string, updates: Partial<RFP>) => void;
  addBid: (rfpId: string, bid: Bid) => void;
}
```

<!-- From src/components/shared/StatusBadge.tsx: -->
```typescript
export function StatusBadge({ status, variant = 'default' }: {
  status: string;
  variant?: 'default' | 'success' | 'warning' | 'error';
}): JSX.Element
```

<!-- From src/lib/utils.ts: -->
```typescript
export function cn(...inputs: ClassValue[]): string
```

<!-- PlaceholderPage pattern to replace (from src/components/shared/PlaceholderPage.tsx): -->
```typescript
// Uses motion.div with initial={{ opacity: 0, y: 8 }} animate={{ opacity: 1, y: 0 }} transition={{ duration: 0.3, ease: 'easeOut' }}
// Page heading: text-xl font-semibold text-slate-900
// Subtitle: mt-1 text-sm text-slate-500
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SupplierCard component and supplier registry page</name>
  <files>src/components/procurement/SupplierCard.tsx, src/app/suppliers/page.tsx</files>
  <read_first>
    - src/app/suppliers/page.tsx (current placeholder to replace)
    - src/components/shared/PlaceholderPage.tsx (motion pattern to follow)
    - src/components/shared/StatusBadge.tsx (reuse for compliance status)
    - src/lib/mock-data/types.ts (Supplier interface)
    - src/context/ProcurementContext.tsx (useProcurement hook)
    - src/lib/utils.ts (cn utility)
    - .planning/phases/02-procurement-ai-engine/02-UI-SPEC.md (SupplierCard spec)
  </read_first>
  <action>
1. Create `src/components/procurement/SupplierCard.tsx`:
   - `'use client'` directive
   - Props: `{ supplier: Supplier }`
   - Import `Link` from `next/link`, `StatusBadge` from `@/components/shared/StatusBadge`, `cn` from `@/lib/utils`, `Star` from `lucide-react`
   - Wrap entire card in `<Link href={/suppliers/${supplier.id}}>` for navigation
   - Card container: `bg-white rounded-xl shadow-sm border border-slate-200 p-6 hover:shadow-md transition-shadow cursor-pointer`
   - Top row: supplier name in `text-lg font-semibold text-slate-900`
   - Rating row: Star icon (lucide `Star`, size 16, `text-amber-500 fill-amber-500`) + rating value in `text-sm font-medium text-slate-700` + `/100`
   - Compliance: `<StatusBadge>` with variant mapping:
     - `'Compliant'` -> variant `'success'`
     - `'Pending Review'` -> variant `'warning'`
     - `'Non-Compliant'` -> variant `'error'`
   - Certifications: Render each as `<span className="inline-flex items-center px-2 py-0.5 rounded text-xs font-medium bg-slate-100 text-slate-600">{cert}</span>` in a flex-wrap container with gap-1
   - Bottom row: `text-xs text-slate-500` showing `Avg delivery: ${supplier.deliveryAvgDays} days`

2. Replace `src/app/suppliers/page.tsx`:
   - `'use client'` directive
   - Import `motion` from `framer-motion`, `useProcurement` from `@/context/ProcurementContext`, `SupplierCard` from `@/components/procurement/SupplierCard`
   - Wrap in `<motion.div>` with same animation as PlaceholderPage: `initial={{ opacity: 0, y: 8 }} animate={{ opacity: 1, y: 0 }} transition={{ duration: 0.3, ease: 'easeOut' }}`
   - Page heading: `<h1 className="text-xl font-semibold text-slate-900">Supplier Registry</h1>`
   - Subtitle: `<p className="mt-1 text-sm text-slate-500">Browse and manage registered suppliers.</p>`
   - Grid below (mt-6): `<div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">`
   - Map `useProcurement().suppliers` to `<SupplierCard key={supplier.id} supplier={supplier} />`
   - Empty state (if suppliers.length === 0): Show heading "No suppliers found" and body "Supplier data could not be loaded. Try refreshing the page." (per UI-SPEC copywriting)
  </action>
  <verify>
    <automated>cd /Users/ericorodrigues/Desktop/GSD_HackatonVGS && npx tsc --noEmit src/app/suppliers/page.tsx src/components/procurement/SupplierCard.tsx 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/components/procurement/SupplierCard.tsx exists and contains `export function SupplierCard`
    - src/components/procurement/SupplierCard.tsx contains `Link` import from `next/link`
    - src/components/procurement/SupplierCard.tsx contains `href={` with `/suppliers/${supplier.id}`
    - src/components/procurement/SupplierCard.tsx contains `StatusBadge` import
    - src/app/suppliers/page.tsx does NOT contain `PlaceholderPage`
    - src/app/suppliers/page.tsx contains `useProcurement`
    - src/app/suppliers/page.tsx contains `SupplierCard`
    - src/app/suppliers/page.tsx contains `grid grid-cols`
    - src/app/suppliers/page.tsx contains `motion.div`
    - src/app/suppliers/page.tsx contains `No suppliers found` (empty state)
  </acceptance_criteria>
  <done>Supplier registry page shows grid of 8 supplier cards with name, rating star, compliance badge, certifications, and delivery days. Each card links to /suppliers/[id].</done>
</task>

<task type="auto">
  <name>Task 2: Create supplier profile detail page</name>
  <files>src/app/suppliers/[id]/page.tsx</files>
  <read_first>
    - src/lib/mock-data/types.ts (Supplier interface fields)
    - src/lib/mock-data/suppliers.ts (actual data to verify rendering)
    - src/context/ProcurementContext.tsx (useProcurement hook)
    - src/components/shared/StatusBadge.tsx (compliance badge)
    - src/components/shared/PlaceholderPage.tsx (motion pattern)
    - .planning/phases/02-procurement-ai-engine/02-UI-SPEC.md (profile page spec)
    - .planning/phases/02-procurement-ai-engine/02-RESEARCH.md (Next.js 16 use(params) pattern)
  </read_first>
  <action>
Create `src/app/suppliers/[id]/page.tsx`:

1. `'use client'` directive at top.

2. Import `use` from `react`, `motion` from `framer-motion`, `useProcurement` from `@/context/ProcurementContext`, `StatusBadge` from `@/components/shared/StatusBadge`, `ArrowLeft`, `Shield`, `Wallet`, `TrendingUp`, `Clock` from `lucide-react`, `Link` from `next/link`.

3. Page component signature (Next.js 16 pattern):
```typescript
export default function SupplierProfilePage({
  params,
}: {
  params: Promise<{ id: string }>
}) {
  const { id } = use(params)
```

4. Find supplier: `const { suppliers } = useProcurement(); const supplier = suppliers.find(s => s.id === id);`

5. If no supplier found, render: "Supplier not found" message with a `<Link href="/suppliers">` back link.

6. Wrap content in `<motion.div>` with standard animation.

7. Back link at top: `<Link href="/suppliers" className="inline-flex items-center gap-1 text-sm text-slate-500 hover:text-slate-700 mb-4">` with ArrowLeft icon (size 16) + "Back to Suppliers"

8. Profile header card (white bg, rounded-xl, shadow-sm, border, p-6):
   - Supplier name: `text-2xl font-semibold text-slate-900`
   - Compliance badge: `<StatusBadge status={supplier.complianceStatus} variant={...}>` with same variant mapping as SupplierCard
   - Rating display: large text `text-4xl font-bold text-slate-900` showing `supplier.rating` with `/100` in `text-lg text-slate-400`

9. Stats grid (mt-6, grid grid-cols-2 lg:grid-cols-4 gap-4):
   Each stat card: `bg-white rounded-xl border border-slate-200 p-4`
   - Past Performance: TrendingUp icon (text-emerald-500), label "Past Performance", value `${supplier.pastPerformance}/100`
   - Avg Delivery: Clock icon (text-indigo-500), label "Avg Delivery", value `${supplier.deliveryAvgDays} days`
   - Risk Score: Shield icon (text-amber-500), label "Risk Score", value `${supplier.riskScore}/100`
   - Wallet: Wallet icon (text-slate-500), label "Wallet Address", value `${supplier.walletAddress.substring(0, 6)}...${supplier.walletAddress.substring(supplier.walletAddress.length - 4)}`

10. Certifications section (mt-6, white card):
   - Heading: "Certifications" in `text-lg font-semibold text-slate-900`
   - If certifications.length === 0: "No certifications" in text-sm text-slate-500
   - Else: flex-wrap with gap-2, each cert as `<span className="inline-flex items-center px-3 py-1 rounded-full text-sm font-medium bg-indigo-50 text-indigo-700">{cert}</span>`

11. Pricing history section (mt-6, white card):
   - Heading: "Pricing History" in `text-lg font-semibold text-slate-900`
   - Simple SVG sparkline (inline, no separate component):
     - SVG viewBox="0 0 200 50", width 200, height 50
     - Plot pricingHistory array as polyline
     - X spacing: `i * (200 / (pricingHistory.length - 1))`
     - Y: normalize values to 0-50 range: `50 - ((val - min) / (max - min || 1)) * 40 - 5`
     - Polyline: `stroke="rgb(79 70 229)" strokeWidth="2" fill="none"`
     - Below sparkline, show `$${Math.min(...pricingHistory).toLocaleString()} - $${Math.max(...pricingHistory).toLocaleString()}` in text-xs text-slate-500
  </action>
  <verify>
    <automated>cd /Users/ericorodrigues/Desktop/GSD_HackatonVGS && npx tsc --noEmit src/app/suppliers/\\[id\\]/page.tsx 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/app/suppliers/[id]/page.tsx exists
    - src/app/suppliers/[id]/page.tsx contains `'use client'`
    - src/app/suppliers/[id]/page.tsx contains `use(params)` (Next.js 16 pattern)
    - src/app/suppliers/[id]/page.tsx contains `params: Promise<{ id: string }>`
    - src/app/suppliers/[id]/page.tsx contains `useProcurement`
    - src/app/suppliers/[id]/page.tsx contains `suppliers.find`
    - src/app/suppliers/[id]/page.tsx contains `StatusBadge`
    - src/app/suppliers/[id]/page.tsx contains `walletAddress`
    - src/app/suppliers/[id]/page.tsx contains `pricingHistory`
    - src/app/suppliers/[id]/page.tsx contains `<svg` (sparkline)
    - src/app/suppliers/[id]/page.tsx contains `polyline` or `path` (sparkline drawing)
    - src/app/suppliers/[id]/page.tsx contains `Supplier not found` (404 state)
  </acceptance_criteria>
  <done>Supplier profile page at /suppliers/[id] shows full supplier details: name, rating, compliance badge, past performance, delivery, risk, wallet address, certifications list, and pricing sparkline. Back link returns to registry.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes for all files
- /suppliers page renders grid of 8 supplier cards
- Clicking any card navigates to /suppliers/[id] with full profile
- Profile page uses Next.js 16 `use(params)` pattern
</verification>

<success_criteria>
- Supplier registry lists all 8 suppliers in responsive grid with name, rating, compliance, certifications
- Each card links to profile page via /suppliers/[id]
- Profile page shows all supplier fields including sparkline for pricing history
- Both pages use framer-motion animation consistent with Phase 1
</success_criteria>

<output>
After completion, create `.planning/phases/02-procurement-ai-engine/02-02-SUMMARY.md`
</output>
