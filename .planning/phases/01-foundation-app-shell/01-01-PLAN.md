---
phase: 01-foundation-app-shell
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - tailwind.config.ts
  - postcss.config.mjs
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/app/globals.css
  - src/app/dashboard/page.tsx
  - src/app/suppliers/page.tsx
  - src/app/rfp/page.tsx
  - src/app/payment/page.tsx
  - src/app/notifications/page.tsx
  - src/app/audit/page.tsx
  - src/app/bids/page.tsx
  - src/app/transactions/page.tsx
  - src/components/layout/AppShell.tsx
  - src/components/layout/Sidebar.tsx
  - src/components/layout/Header.tsx
  - src/components/shared/PlaceholderPage.tsx
  - src/components/shared/StatusBadge.tsx
autonomous: true
requirements:
  - FOUN-01
  - FOUN-02
must_haves:
  truths:
    - "App loads in browser at localhost:3000 with sidebar, header, and main content area"
    - "Sidebar shows navigation links with lucide-react icons"
    - "Header shows 'GovProcure AI' logo and notification bell"
    - "Clicking any nav link renders the corresponding placeholder page without errors"
    - "All placeholder pages show heading, subtitle, and empty state card"
  artifacts:
    - path: "package.json"
      provides: "Project dependencies"
      contains: "framer-motion"
    - path: "src/app/layout.tsx"
      provides: "Root layout with Inter font"
      contains: "Inter"
    - path: "src/components/layout/AppShell.tsx"
      provides: "Shell wrapper with sidebar + header + main"
      exports: ["AppShell"]
    - path: "src/components/layout/Sidebar.tsx"
      provides: "Fixed sidebar with nav links"
      exports: ["Sidebar"]
    - path: "src/components/layout/Header.tsx"
      provides: "Fixed header with logo and bell"
      exports: ["Header"]
    - path: "src/components/shared/PlaceholderPage.tsx"
      provides: "Reusable placeholder page component"
      exports: ["PlaceholderPage"]
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/components/layout/AppShell.tsx"
      via: "import and render as wrapper"
      pattern: "import.*AppShell"
    - from: "src/components/layout/AppShell.tsx"
      to: "src/components/layout/Sidebar.tsx"
      via: "renders Sidebar component"
      pattern: "<Sidebar"
    - from: "src/app/dashboard/page.tsx"
      to: "src/components/shared/PlaceholderPage.tsx"
      via: "renders PlaceholderPage with props"
      pattern: "<PlaceholderPage"
---

<objective>
Scaffold the Next.js 14 project with all Phase 1 dependencies, build the app shell layout (sidebar, header, main content area), create all placeholder pages with routing, and build shared components.

Purpose: Establish the running application foundation that all subsequent phases build upon. After this plan, `npm run dev` shows a navigable app with sidebar and header.
Output: A working Next.js app with layout shell, 8 routed placeholder pages, and shared components.
</objective>

<execution_context>
@/Users/ericorodrigues/Desktop/GSD_HackatonVGS/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ericorodrigues/Desktop/GSD_HackatonVGS/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-app-shell/01-UI-SPEC.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js project and install all dependencies</name>
  <files>package.json, tsconfig.json, tailwind.config.ts, postcss.config.mjs, src/app/layout.tsx, src/app/page.tsx, src/app/globals.css</files>
  <read_first>
    - .planning/research/STACK.md (package versions and stack decisions)
    - .planning/phases/01-foundation-app-shell/01-UI-SPEC.md (design system tokens, font, colors)
  </read_first>
  <action>
1. Run `npx create-next-app@latest govprocure-ai --typescript --tailwind --eslint --app --src-dir --no-import-alias` from the project root. If the command prompts, answer: TypeScript=Yes, ESLint=Yes, Tailwind=Yes, src/=Yes, App Router=Yes, import alias=No.

   IMPORTANT: After scaffolding, move ALL contents from the `govprocure-ai/` subdirectory up to the project root and delete the empty subdirectory. The app must live at repo root, not in a subfolder.

2. Install additional dependencies:
   ```
   npm install framer-motion@^11.0.0 lucide-react@^0.400.0 clsx@^2.1.1 tailwind-merge@^2.3.0 react-hot-toast@^2.4.1 date-fns@^3.6.0 uuid@^9.0.1
   npm install -D @types/uuid
   ```

3. Create `src/lib/utils.ts` with the `cn` utility function:
   ```typescript
   import { clsx, type ClassValue } from 'clsx';
   import { twMerge } from 'tailwind-merge';

   export function cn(...inputs: ClassValue[]) {
     return twMerge(clsx(inputs));
   }
   ```

4. Update `src/app/globals.css` to contain only:
   ```css
   @tailwind base;
   @tailwind components;
   @tailwind utilities;
   ```

5. Update `src/app/layout.tsx` to load Inter font via `next/font/google` with `subsets: ['latin'], weight: ['400', '600']`. Set the html className to the Inter font variable. Set metadata title to "GovProcure AI" and description to "AI-Powered Government Procurement Platform".

6. Update `src/app/page.tsx` to redirect to `/dashboard` using `redirect` from `next/navigation`:
   ```typescript
   import { redirect } from 'next/navigation';
   export default function Home() {
     redirect('/dashboard');
   }
   ```

7. Verify `tailwind.config.ts` content array includes `'./src/**/*.{js,ts,jsx,tsx,mdx}'`.
  </action>
  <verify>
    <automated>cd /Users/ericorodrigues/Desktop/GSD_HackatonVGS && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains "framer-motion" in dependencies
    - package.json contains "lucide-react" in dependencies
    - package.json contains "clsx" in dependencies
    - package.json contains "tailwind-merge" in dependencies
    - package.json contains "react-hot-toast" in dependencies
    - package.json contains "date-fns" in dependencies
    - package.json contains "uuid" in dependencies
    - src/app/layout.tsx contains "Inter" from "next/font/google"
    - src/app/layout.tsx contains 'weight: ["400", "600"]' or "weight: ['400', '600']"
    - src/app/layout.tsx contains "GovProcure AI" in metadata
    - src/app/page.tsx contains 'redirect("/dashboard")' or "redirect('/dashboard')"
    - src/lib/utils.ts contains "export function cn"
    - src/app/globals.css contains "@tailwind base"
    - npm run build completes without errors
  </acceptance_criteria>
  <done>Next.js 14 app scaffolded at project root with all Phase 1 dependencies installed, Inter font loaded with weights 400 and 600, root page redirects to /dashboard, cn utility exists, and `npm run build` succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Build app shell layout components and all placeholder pages</name>
  <files>
    src/components/layout/AppShell.tsx,
    src/components/layout/Sidebar.tsx,
    src/components/layout/Header.tsx,
    src/components/shared/PlaceholderPage.tsx,
    src/components/shared/StatusBadge.tsx,
    src/app/layout.tsx,
    src/app/dashboard/page.tsx,
    src/app/suppliers/page.tsx,
    src/app/rfp/page.tsx,
    src/app/payment/page.tsx,
    src/app/notifications/page.tsx,
    src/app/audit/page.tsx,
    src/app/bids/page.tsx,
    src/app/transactions/page.tsx
  </files>
  <read_first>
    - src/app/layout.tsx (current state after Task 1)
    - .planning/phases/01-foundation-app-shell/01-UI-SPEC.md (exact styles, nav items, copy, animation specs)
    - .planning/research/ARCHITECTURE.md (component map and file locations)
    - src/lib/utils.ts (cn utility for class merging)
  </read_first>
  <action>
1. Create `src/components/layout/Sidebar.tsx` as a CLIENT component ("use client"):
   - Fixed positioning: `fixed left-0 top-16 w-64 h-[calc(100vh-4rem)] bg-slate-900 border-r border-slate-800 z-20`
   - Accept `role` prop typed as `'gov' | 'supplier' | 'auditor'`
   - Accept `currentPath` prop typed as `string`
   - Define nav items per role as a constant object:
     ```typescript
     const NAV_ITEMS: Record<string, Array<{ label: string; href: string; icon: React.ComponentType<{ className?: string }> }>> = {
       gov: [
         { label: 'Dashboard', href: '/dashboard', icon: LayoutDashboard },
         { label: 'Suppliers', href: '/suppliers', icon: Users },
         { label: 'Procurement', href: '/rfp', icon: FileText },
         { label: 'Payments', href: '/payment', icon: CreditCard },
         { label: 'Notifications', href: '/notifications', icon: Bell },
       ],
       supplier: [
         { label: 'Dashboard', href: '/dashboard', icon: LayoutDashboard },
         { label: 'My Bids', href: '/bids', icon: FileText },
         { label: 'Notifications', href: '/notifications', icon: Bell },
       ],
       auditor: [
         { label: 'Dashboard', href: '/dashboard', icon: LayoutDashboard },
         { label: 'Audit Trail', href: '/audit', icon: Shield },
         { label: 'Transactions', href: '/transactions', icon: Receipt },
         { label: 'Notifications', href: '/notifications', icon: Bell },
       ],
     };
     ```
   - Import icons from `lucide-react`: LayoutDashboard, Users, FileText, CreditCard, Bell, Shield, Receipt
   - Use Next.js `Link` from `next/link` for nav items
   - Nav item default style: `flex items-center gap-3 px-3 py-2 rounded-lg text-slate-400 hover:text-slate-50 hover:bg-slate-800 transition-colors duration-150 text-xs`
   - Nav item active style (when `href === currentPath`): `flex items-center gap-3 px-3 py-2 rounded-lg text-white bg-indigo-600 font-semibold text-xs`
   - Icon size classes: `w-5 h-5`
   - Add padding `p-4` to the nav container, with `space-y-1` between items

2. Create `src/components/layout/Header.tsx` as a CLIENT component ("use client"):
   - Fixed positioning: `fixed top-0 left-0 right-0 h-16 bg-slate-900 border-b border-slate-700 z-30`
   - Flex layout: `flex items-center justify-between px-6`
   - Left side: Logo text "GovProcure AI" using `text-lg font-semibold text-slate-50`
   - Right side: `flex items-center gap-4`
     - A placeholder div for role switcher (will be wired in Plan 01-02): `<div id="role-switcher-slot" />`
     - Notification bell button: `<button aria-label="Notifications" className="text-slate-400 hover:text-slate-50 transition-colors"><Bell className="w-5 h-5" /></button>`
   - Import `Bell` from `lucide-react`

3. Create `src/components/layout/AppShell.tsx` as a CLIENT component ("use client"):
   - Import Sidebar and Header
   - Use `usePathname()` from `next/navigation` to get current path
   - For now, hardcode role as `'gov'` (Plan 01-02 will wire to context)
   - Render structure:
     ```tsx
     <div className="min-h-screen bg-slate-50">
       <Header />
       <Sidebar role={role} currentPath={pathname} />
       <main className="ml-64 mt-16 p-8 min-h-screen">
         {children}
       </main>
     </div>
     ```
   - Accept `children: React.ReactNode` prop

4. Create `src/components/shared/PlaceholderPage.tsx` as a CLIENT component ("use client"):
   - Props: `heading: string`, `subtitle: string`
   - Use Framer Motion for page mount animation: `motion.div` with `initial={{ opacity: 0, y: 8 }}`, `animate={{ opacity: 1, y: 0 }}`, `transition={{ duration: 0.3, ease: 'easeOut' }}`
   - Render:
     - `<h1 className="text-xl font-semibold text-slate-900">{heading}</h1>`
     - `<p className="mt-1 text-sm text-slate-500">{subtitle}</p>`
     - Empty state card: `<div className="mt-8 bg-white rounded-xl shadow-md border border-slate-200 p-8 text-center">`
       - Inside card: `<p className="text-base font-semibold text-slate-900">Nothing here yet</p>`
       - `<p className="mt-2 text-sm text-slate-500">This section is under construction. Check back after the next phase.</p>`

5. Create `src/components/shared/StatusBadge.tsx`:
   - Props: `status: string`, `variant?: 'default' | 'success' | 'warning' | 'error'`
   - Variant color map:
     - default: `bg-slate-100 text-slate-600`
     - success: `bg-emerald-50 text-emerald-600`
     - warning: `bg-amber-50 text-amber-600`
     - error: `bg-red-50 text-red-600`
   - Render: `<span className="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-semibold {variantClasses}">{status}</span>`

6. Update `src/app/layout.tsx` to wrap children with `<AppShell>`:
   ```tsx
   import { AppShell } from '@/components/layout/AppShell';
   // ... existing Inter font setup ...
   export default function RootLayout({ children }: { children: React.ReactNode }) {
     return (
       <html lang="en" className={inter.className}>
         <body>
           <AppShell>{children}</AppShell>
         </body>
       </html>
     );
   }
   ```

7. Create all 8 placeholder pages using exact copy from UI-SPEC:
   - `src/app/dashboard/page.tsx`: PlaceholderPage heading="Dashboard" subtitle="Financial overview and procurement metrics will appear here."
   - `src/app/suppliers/page.tsx`: PlaceholderPage heading="Supplier Registry" subtitle="Browse and manage registered suppliers."
   - `src/app/rfp/page.tsx`: PlaceholderPage heading="Procurement Requests" subtitle="Create and track RFPs across all stages."
   - `src/app/payment/page.tsx`: PlaceholderPage heading="Payments" subtitle="Payment checkout and settlement tracking will appear here."
   - `src/app/notifications/page.tsx`: PlaceholderPage heading="Notifications" subtitle="Payment and procurement event history will appear here."
   - `src/app/audit/page.tsx`: PlaceholderPage heading="Audit Trail" subtitle="Timestamped compliance records and export tools will appear here."
   - `src/app/bids/page.tsx`: PlaceholderPage heading="My Bids" subtitle="Track your submitted bids and their evaluation status."
   - `src/app/transactions/page.tsx`: PlaceholderPage heading="Transactions" subtitle="View all completed transactions across procurement requests."

   Each page file must have `"use client"` at the top and import PlaceholderPage from `@/components/shared/PlaceholderPage`.
  </action>
  <verify>
    <automated>cd /Users/ericorodrigues/Desktop/GSD_HackatonVGS && npm run build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - src/components/layout/Sidebar.tsx contains "use client" on line 1
    - src/components/layout/Sidebar.tsx contains "NAV_ITEMS" as a constant
    - src/components/layout/Sidebar.tsx contains "LayoutDashboard" imported from "lucide-react"
    - src/components/layout/Sidebar.tsx contains "bg-indigo-600" for active state
    - src/components/layout/Sidebar.tsx contains "bg-slate-900" for sidebar background
    - src/components/layout/Sidebar.tsx contains "w-64" for sidebar width
    - src/components/layout/Header.tsx contains 'aria-label="Notifications"'
    - src/components/layout/Header.tsx contains "GovProcure AI"
    - src/components/layout/Header.tsx contains "h-16" and "bg-slate-900"
    - src/components/layout/Header.tsx contains "role-switcher-slot" placeholder
    - src/components/layout/AppShell.tsx contains "usePathname" from "next/navigation"
    - src/components/layout/AppShell.tsx contains "ml-64 mt-16 p-8"
    - src/components/shared/PlaceholderPage.tsx contains "motion.div" from "framer-motion"
    - src/components/shared/PlaceholderPage.tsx contains "opacity: 0, y: 8" as initial animation values
    - src/components/shared/PlaceholderPage.tsx contains "Nothing here yet"
    - src/components/shared/StatusBadge.tsx contains "rounded-full text-xs font-semibold"
    - src/app/dashboard/page.tsx contains 'heading="Dashboard"'
    - src/app/suppliers/page.tsx contains 'heading="Supplier Registry"'
    - src/app/rfp/page.tsx contains 'heading="Procurement Requests"'
    - src/app/audit/page.tsx contains 'heading="Audit Trail"'
    - src/app/bids/page.tsx contains 'heading="My Bids"'
    - src/app/transactions/page.tsx contains 'heading="Transactions"'
    - src/app/layout.tsx contains "AppShell"
    - npm run build completes without errors
  </acceptance_criteria>
  <done>App shell renders with fixed sidebar (dark, w-64, nav links with icons), fixed header (dark, h-16, logo + bell), and main content area (light, ml-64 mt-16). All 8 placeholder pages render via routing. PlaceholderPage has Framer Motion mount animation. StatusBadge component exists. `npm run build` succeeds.</done>
</task>

</tasks>

<verification>
1. `npm run build` completes without errors
2. `npm run dev` starts and loads at http://localhost:3000
3. Browser redirects from / to /dashboard
4. Sidebar shows 5 nav links (Gov Officer set, hardcoded for now)
5. Clicking each nav link shows corresponding placeholder page with correct heading text
6. Header shows "GovProcure AI" logo and notification bell with aria-label
7. PlaceholderPage animates on mount (opacity + y-translate)
8. No console errors in browser
</verification>

<success_criteria>
- Next.js 14 app with App Router running at localhost:3000
- All Phase 1 dependencies installed (framer-motion, lucide-react, clsx, tailwind-merge, react-hot-toast, date-fns, uuid)
- Inter font loaded with weights 400 and 600
- App shell with dark sidebar (w-64, bg-slate-900), dark header (h-16, bg-slate-900), light main content (bg-slate-50)
- 8 routed placeholder pages all render without errors
- Sidebar nav links navigate between pages
- PlaceholderPage and StatusBadge shared components exist and export correctly
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-app-shell/01-01-SUMMARY.md`
</output>
