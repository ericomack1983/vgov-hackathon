'use client';

import { PlaceholderPage } from '@/components/shared/PlaceholderPage';

export default function RfpPage() {
  return (
    <PlaceholderPage
      heading="Procurement Requests"
      subtitle="Create and track RFPs across all stages."
    />
  );
}
